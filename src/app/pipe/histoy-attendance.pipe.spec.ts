import { HistoyAttendancePipe } from './histoy-attendance.pipe';

describe('HistoyAttendancePipe', () => {
  it('create an instance', () => {
    const pipe = new HistoyAttendancePipe();
    expect(pipe).toBeTruthy();
  });
});
